name: "Élise Investigator 0.2 Test"
version: "0.2.0-dev.28"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator 0.2, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-dev28-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# Dev.28 conserve intégralement l'exploration MCP bornée validée en dev.26
# et le bouton Texte validé en dev.27. Seule évolution : la carte MCP possède
# son propre sélecteur d'objet, alimenté par le même catalogue read-only.
# Investigator n'a plus besoin d'être exécuté avant une recherche MCP.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?
