name: "Élise Investigator 0.2 Test"
version: "0.3.0-rc.12"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-v2-rc12-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# V2 RC12 TERRAIN : compléter le déclencheur runtime après la projection exacte RC11.
# Conserve toute cause déjà résolue ; sinon exige une commande exécutée unique et complète la chaîne sur une copie.
# result.params reste prioritaire ; configuration uniquement au chemin exécuté et résolution exacte par registre.
# Le déclencheur vient exclusivement de la trace runtime ; une barrière temporelle antérieure continue de bloquer son réemploi.
# Projection RC11, sélection des traces, resolver default/delay, renderer et dev.54 inchangés.
# Qualification : 413/413 tests PASS ; CI et image privée amd64 vérifiées ; terrain RC12 encore à valider.
# Image : ghcr.io/brunofoxmulder/elise-investigator-v2-rc12-private:0.3.0-rc.12
# Recette prioritaire : ON naturels aspirateur et brosse à dents, puis leurs OFF et les cas déjà validés.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?

