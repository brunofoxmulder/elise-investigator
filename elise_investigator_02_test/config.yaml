name: "Élise Investigator 0.2 Test"
version: "0.2.0-dev.56"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator 0.2, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-dev56-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# DEV.56 TERRAIN 2026-09-05 : causalité native Logbook de premier niveau.
# Base : dev.55 terrain figée au commit 16e3a911e8268aac4b76d074f88299c4c8324732.
# Dev.55 conserve le filtrage unavailable/unknown et le dernier vrai changement fonctionnel.
# Dev.56 promeut context_source/context_message lorsqu'une automation/script est confirmée mais reason resterait null.
# Trace ciblée conservée pour temporisations, wait_for_trigger, choose/default, conditions et décisions runtime.
# Image : ghcr.io/brunofoxmulder/elise-investigator-dev56-private:0.2.0-dev.56
# CI : tests + compilation PASS. App strictement en lecture seule vis-à-vis de Home Assistant.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?
