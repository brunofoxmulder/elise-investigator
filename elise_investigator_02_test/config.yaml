name: "Élise Investigator 0.2 Test"
version: "0.3.0-rc.10"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-v2-rc10-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# V2 RC10 TERRAIN : correction minimale de normalisation après les KO RC9 brosse à dents OFF et aspirateur OFF.
# Cause racine confirmée : la configuration brute des traces d'automatisation Home Assistant utilise la clé racine `action` au singulier, alors que le helper de navigation `_config_at_path()` ne lisait que `actions`.
# RC10 accepte désormais les deux formes structurées `action` et `actions`, sans recherche libre ni rapprochement temporel.
# Les fixtures brosse à dents OFF (delay 3601 s -> turn_off) et aspirateur OFF (choose -> choice: default -> turn_off) utilisent maintenant la forme réelle HA `action`.
# Le resolver V2 général, la sélection de trace RC7/RC8/RC9, le rendu avec âge relatif de l'événement et les chemins déjà validés restent inchangés.
# Aucun nouveau support climate/attribut dans cette RC. Dev.54 reste le fallback officiel intact.
# Image : ghcr.io/brunofoxmulder/elise-investigator-v2-rc10-private:0.3.0-rc.10
# CI complète RC10 : PASS. Image privée amd64 : PASS. Validation terrain prioritaire : brosse à dents OFF et aspirateur OFF.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?
