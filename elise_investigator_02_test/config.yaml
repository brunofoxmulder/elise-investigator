name: "Élise Investigator 0.2 Test"
version: "0.2.0-dev.58"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator 0.2, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-dev58-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# DEV.58 TERRAIN : Home Assistant 2026.9 Activity reste la source causale primaire.
# Sélection fonctionnelle héritée de dev.55 : unknown/unavailable ne remplacent pas le dernier vrai changement.
# Correction dev.58 : pour une question d'état simple sur une lampe, un changement de luminosité ultérieur ne masque plus le dernier vrai OFF↔ON.
# La ou les causes natives Activity/Logbook sont reprises telles que Home Assistant les expose.
# Si Activity identifie l'automatisation mais omet la raison interne de l'action, seule la trace de cette exécution est consultée.
# Aucun graphe causal ni recherche inverse générale n'est reconstruit.
# Période de consultation cible : 12 h par défaut, réglable au niveau de la requête.
# Image : ghcr.io/brunofoxmulder/elise-investigator-dev58-private:0.2.0-dev.58
# CI + build/push + vérification manifeste : PASS. App strictement en lecture seule vis-à-vis de Home Assistant.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?
