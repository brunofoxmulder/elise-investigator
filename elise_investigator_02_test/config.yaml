name: "Élise Investigator 0.2 Test"
version: "0.2.0-dev.55"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator 0.2, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-dev55-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# DEV.55 TERRAIN 2026-09-05 : causalité native Home Assistant d'abord.
# Base exacte dev.54 validée ; repli officiel : dev54-fallback-stable.
# Les interruptions unavailable/unknown ne remplacent plus un vrai changement fonctionnel.
# Les volets conservent leurs épisodes opening/closing ; HA Voice dev.54 reste inchangé.
# Image : ghcr.io/brunofoxmulder/elise-investigator-dev55-private:0.2.0-dev.55
# CI : 225/225 tests PASS. App strictement en lecture seule vis-à-vis de Home Assistant.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?
