name: "Élise Investigator 0.2 Test"
version: "0.2.0-dev.64.1"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator 0.2, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-dev64-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# DEV.64.1 TERRAIN : logique causale identique à dev.63 ; correction de packaging uniquement.
# Launcher dédié run_dev64.sh, invoqué explicitement par /usr/bin/with-contenv bashio.
# Cela conserve SUPERVISOR_TOKEN sans exécuter directement le script, ce qui évite le Permission denied vu en dev.64.
# Le publisher construit d'abord une image locale amd64 et vérifie le wrapper Home Assistant + launcher avant publication.
# Image : ghcr.io/brunofoxmulder/elise-investigator-dev64-private:0.2.0-dev.64.1
# Tests + packaging checks + build/push + vérification manifeste : PASS. Lecture seule vis-à-vis de Home Assistant.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?
