name: "Élise Investigator 0.2 Test"
version: "0.2.0-dev.67"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator 0.2, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-dev67-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# DEV.67 TERRAIN : logique causale identique à dev.63 ; retour au packaging exact réellement validé en dev.62.
# Dockerfile générique dev.62 inchangé : COPY run.sh /run.sh ; RUN chmod 0755 /run.sh ; CMD ["/run.sh"].
# run.sh conserve le shebang #!/usr/bin/with-contenv bashio et lance uniquement main_dev67.py.
# Aucun Dockerfile.devXX ni run_devXX.sh n'est utilisé pour cette image.
# Image : ghcr.io/brunofoxmulder/elise-investigator-dev67-private:0.2.0-dev.67
# Tests unitaires/digital-twin : PASS. Contrat packaging dev.62 vérifié : PASS. Build/push/manifeste : PASS.
# App strictement en lecture seule vis-à-vis de Home Assistant. Dev.54 reste le fallback officiel.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?
