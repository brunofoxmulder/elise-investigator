name: "Élise Investigator 0.2 Test"
version: "0.2.0-dev.66"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator 0.2, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-dev66-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# DEV.66 TERRAIN : logique causale identique à dev.63/dev.64/dev.65 ; correction de packaging uniquement.
# Entrypoint dédié main_dev66.py, launcher dédié run_dev66.sh, Dockerfile dédié Dockerfile.dev66.
# Le launcher est rendu exécutable dans l'image par RUN chmod 0755 /run_dev66.sh.
# Le publisher construit d'abord l'image locale, vérifie le bit exécutable, le wrapper with-contenv,
# puis exécute réellement le launcher et refuse toute occurrence "Permission denied" avant publication.
# Image : ghcr.io/brunofoxmulder/elise-investigator-dev66-private:0.2.0-dev.66
# Tests + packaging checks + exécution launcher + build/push + vérification manifeste : PASS.
# App strictement en lecture seule vis-à-vis de Home Assistant. Dev.54 reste le fallback officiel.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?
