name: "Élise Investigator 0.2 Test"
version: "0.2.0-dev.65"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator 0.2, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-dev65-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# DEV.65 TERRAIN : logique causale identique à dev.63/dev.64 ; correction de packaging/version uniquement.
# Entrypoint dédié main_dev65.py, launcher dédié run_dev65.sh, Dockerfile dédié Dockerfile.dev65.
# Lancement explicite via /usr/bin/with-contenv bashio /run_dev65.sh afin de conserver SUPERVISOR_TOKEN
# sans dépendre du bit exécutable du launcher. Version entière suivante pour être correctement reconnue par HA.
# Image : ghcr.io/brunofoxmulder/elise-investigator-dev65-private:0.2.0-dev.65
# Tests + packaging checks + build/push + vérification manifeste : PASS. Lecture seule vis-à-vis de Home Assistant.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?
