name: "Élise Investigator 0.2 Test"
version: "0.2.0-dev.70"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator 0.2, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-dev70-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# DEV.70 TERRAIN : correction bornée issue de la recette naturelle dev.69.
# 1) Conserve le vrai fait terminal open/closed mais écarte les refreshs successifs du même état quand la frontière fonctionnelle est visible.
# 2) Permet au mouvement opening/closing du même épisode natif de porter l'attribution Activity.
# 3) Si la trace exacte ne donne que time_pattern, préfère les conditions runtime prouvées de la branche exécutée contenant la commande cible.
# 4) Conserve trigger state/numeric_state + conditions de branche lorsqu'ils sont conjointement nécessaires, ex. heures creuses + batterie basse.
# 5) Conserve la logique dev.68 pour wait/delay/action finale. Fail closed si ambigu ou non prouvé.
# Non-régressions : Tineco OFF >99,9 %, chargeur téléphone 2 OFF >99 %, entrée OFF, lampes simples/manuelles,
# fermetures coucher du soleil et chaîne profonde lampe cuisine OFF <- ouverture volet <- conditions météo.
# Présentation française, hiérarchisation cause/guards et fonction « depuis quand ? » restent hors périmètre.
# Aucun graphe, aucune corrélation temporelle générale, aucune recherche causale large.
# Packaging EXACT terrain dev.62/dev.67/dev.69 : Dockerfile générique + /run.sh + chmod 0755 + CMD ["/run.sh"].
# Aucun Dockerfile.dev70 ni run_dev70.sh. Image : ghcr.io/brunofoxmulder/elise-investigator-dev70-private:0.2.0-dev.70
# Tests unitaires/digital-twin : PASS. Contrat packaging : PASS. Build/push/manifeste : PASS.
# Le démarrage HAOS + Ingress reste à valider sur le terrain. Lecture seule. Dev.54 reste le fallback officiel.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?
