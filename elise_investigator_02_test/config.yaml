name: "Élise Investigator 0.2 Test"
version: "0.2.0-dev.43"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator 0.2, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-dev43-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# Dev.43 embarque les corrections cover dev.39 et le contrat de facteurs structurés
# dev.40-dev.43. Les réponses legacy restent pilotées par reason ; factors est additif.
# Les causes combinées ne sont produites que par une règle conservative : prédicats
# présents comme triggers configurés + conditions runtime vraies sur le chemin exécuté.
# Les gardes vraies ordinaires ne sont pas automatiquement promues en causes.
# Métadonnées métier éventuelles : libellé seulement, jamais preuve causale.
# L'App reste strictement en lecture seule vis-à-vis de Home Assistant.
# La mémoire consciente reste isolée dans /data/conscious_memory.sqlite3.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?
