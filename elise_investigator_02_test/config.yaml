name: "Élise Investigator 0.2 Test"
version: "0.3.0-rc.8"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-v2-rc8-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# V2 RC8 TERRAIN : deux corrections structurelles ciblées après RC7.
# 1) Les triggers Home Assistant de type device turned_on / turned_off sont désormais rendus à partir de la preuve runtime même sans to_state.
# 2) Un wait_for_trigger terminé peut expliquer l'action qui le suit immédiatement dans la même branche exécutée choose/.../sequence/....
# Cette extension reste strictement action-locale : commande d'effet unique, même branche exécutée, wait frère immédiatement précédent ; aucun rapprochement temporel libre.
# La sélection de trace renforcée de RC7 est conservée sans modification.
# Le resolver causal V2 général, le rendu avec âge relatif de l'événement et les chemins déjà validés restent inchangés.
# Aucun nouveau support climate/attribut dans cette RC. Dev.54 reste le fallback officiel intact.
# Image : ghcr.io/brunofoxmulder/elise-investigator-v2-rc8-private:0.3.0-rc.8
# CI complète : PASS. Image privée amd64 + manifeste : PASS. Validation terrain prioritaire : brosse à dents ON et aspirateur OFF.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?
