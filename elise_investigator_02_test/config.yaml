name: "Élise Investigator 0.2 Test"
version: "0.2.0-dev.49"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator 0.2, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-dev49-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# Dev.49 conserve l'architecture et la logique cover validées jusqu'en dev.48.
# Correction 1 : pour les questions d'état génériques non-cover, les transitions
# techniques liées à unknown/unavailable ne remplacent plus le dernier vrai off↔on.
# Correction 2 : lorsqu'un contexte Home Assistant a déjà prouvé l'automatisation
# ou le script à l'origine d'un vrai off↔on, la trace de cette source peut être relue
# pour récupérer la raison fonctionnelle même si une device action n'expose pas
# l'entity_id canonique dans la cible de service.
# Les entités cover.* sont exclues du fallback générique et conservent strictement
# les épisodes ouverture/fermeture/position déjà validés.
# Qualification avant terrain : replay Tineco, chargeur téléphone, yaourtière,
# aspirateur et garde-fou volets ; suite complète 230/230 tests PASS.
# L'App reste strictement en lecture seule vis-à-vis de Home Assistant.
# La mémoire consciente reste isolée dans /data/conscious_memory.sqlite3.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?
