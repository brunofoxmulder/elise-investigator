name: "Élise Investigator 0.2 Test"
version: "0.2.0-dev.68"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator 0.2, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-dev68-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# DEV.68 TERRAIN : conserve tous les sélecteurs dev.67 et ajoute seulement trois formes bornées dans la trace exacte.
# 1) wait_for_trigger terminé immédiatement avant l'action cible, même avec une seule commande cible.
# 2) delay exécuté immédiatement avant l'action cible.
# 3) trigger de départ + conditions top-level toutes prouvées vraies lorsqu'elles sont conjointement nécessaires.
# Aucun graphe, aucune corrélation temporelle générale, aucune recherche causale large. Fail closed si ambigu.
# Présentation des textes bruts et fonction « depuis quand ? » restent hors périmètre de dev.68.
# Packaging EXACT dev.62/dev.67 : Dockerfile générique + /run.sh + chmod 0755 + CMD ["/run.sh"].
# Aucun Dockerfile.dev68 ni run_dev68.sh. Image : ghcr.io/brunofoxmulder/elise-investigator-dev68-private:0.2.0-dev.68
# Tests unitaires/digital-twin : PASS. Contrat packaging : PASS. Build/push/manifeste : PASS.
# Le démarrage HAOS reste à valider sur le terrain ; ne pas confondre CI et preuve terrain.
# App strictement en lecture seule vis-à-vis de Home Assistant. Dev.54 reste le fallback officiel.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?
