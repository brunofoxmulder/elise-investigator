name: "Élise Investigator 0.2 Test"
version: "0.2.0-dev.53"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator 0.2, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-dev53-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# ROLLBACK COMPLET TERRAIN 2026-09-01 : retour au niveau fonctionnel d'avant dev.47.
# Runtime exact : main_dev46.py depuis le commit dev.46
# 1fa3e63852a3ce701b1f230fa0311942d7589d2f.
# Restaure le stockage historique dev.46 : /data/conscious_memory.sqlite3
# et le fonctionnement mémoire/MCP/volets/lumières/enrichissement associé.
# Les fichiers temporaires dev.52 sont nettoyés au démarrage sans toucher à l'ancienne mémoire.
# Qualification dev.53 : tests PASS, build/publish PASS, manifeste OCI vérifié.
# Image : ghcr.io/brunofoxmulder/elise-investigator-dev53-private:0.2.0-dev.53
# Digest OCI : sha256:28ee52e8da8f2c1984c0e2cda78cdddc1559ec8d4c6bbd2c8bd52422f53abc39
# L'App reste strictement en lecture seule vis-à-vis de Home Assistant.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?
