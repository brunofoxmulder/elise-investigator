name: "Élise Investigator 0.2 Test"
version: "0.3.0-rc.13"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-v2-rc13-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# V2 RC13 : conserver les preuves exactes lorsque les traces HA disparaissent.
# Table additive dans le SQLite existant ; identite stricte de l'evenement selectionne par le journal.
# Collecte bornee sur le flux existant ; aucune commande HA ; resolver et renderer RC12 conserves.
# Retention selon reglage existant (12 h par defaut, 1 a 72 h) ; plafonds et conflits geres.
# Qualification : 471/471 tests PASS ; CI, build amd64 et manifeste verifies ; terrain RC13 a valider.
# Candidate : fbd7e1566c50cc91bafe103335e64f9c10f70cf9 ; PR #87 brouillon, non fusionnee.
# Image : ghcr.io/brunofoxmulder/elise-investigator-v2-rc13-private:0.3.0-rc.13
# Digest OCI : sha256:551d28c975f57139cc7fc4aa1d0e39507a5115684c4d80510f0b02c84c7f26da
# Recette : demarrage/Ingress, mouvement naturel sans question, rotation traces, relecture et redemarrage.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?

