name: "Élise Investigator 0.2 Test"
version: "0.2.0-dev.26"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator 0.2, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-dev26-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# Dev.26: conserve la synthèse déterministe dev.24 et le garde-fou de contrat
# dev.25. Lors d'une recherche MCP explicite seulement, l'app peut interroger
# au plus 6 candidats, 3 traces récentes chacun et 1 détail compact. La proximité
# temporelle n'est jamais une preuve causale, aucun LLM n'est utilisé et le
# verdict Investigator reste inchangé.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?
