name: "Élise Investigator 0.2 Test"
version: "0.3.0-rc.9"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-v2-rc9-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# V2 RC9 TERRAIN : corrections ciblées à partir des structures réelles confirmées par les traces HA et la documentation Maison Cognitive.
# 1) Brosse à dents OFF : une action device turn_off suivant immédiatement un delay réellement exécuté peut être reliée au délai écoulé, même lorsque HA n'expose que la cible device/registry dans la trace runtime.
# 2) Aspirateur OFF : quand HA prouve choice: default sur un choose contenant une seule branche et une seule condition numeric_state, la branche default peut être expliquée par le fait que cette condition n'a pas sélectionné la branche. Aucune valeur runtime n'est inventée.
# Le resolver V2 validé est exécuté en priorité et reste inchangé ; RC9 n'ajoute ces fallbacks qu'en cas d'échec du chemin V2.
# La sélection de trace RC7/RC8, le rendu avec âge relatif de l'événement et les chemins déjà validés restent conservés.
# Aucun nouveau support climate/attribut dans cette RC. Dev.54 reste le fallback officiel intact.
# Image : ghcr.io/brunofoxmulder/elise-investigator-v2-rc9-private:0.3.0-rc.9
# CI complète RC9 : PASS. Image privée amd64 + manifeste : PASS. Validation terrain prioritaire : brosse à dents OFF et aspirateur OFF sur les événements récents déjà observés.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?
