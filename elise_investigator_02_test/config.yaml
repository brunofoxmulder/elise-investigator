name: "Élise Investigator 0.2 Test"
version: "0.2.0-dev.72"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator 0.2, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-dev72-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# DEV.72 TERRAIN : correction ciblée du chemin ha_logbook+exact_trace après KO terrain dev.71.
# Cas reproduit : mouvement déclencheur + conditions state (prise ON, Mode Cinéma OFF) ; les conditions restent des guards et ne sont plus ajoutées à la cause humaine.
# Les causes conjointes déjà validées restent conservées lorsqu'un seuil numeric_state fait partie de la conjonction prouvée (ex. heures creuses + batterie basse).
# Garde-fous dev.71 conservés : provider text "triggered by state of..." non exposé, trigger initial non recyclé après barrière temporelle locale.
# Non-régressions : unavailable/unknown, épisodes cover/volets, délais/waits/timeout, PASS historiques dev.67 -> dev.71.
# Aucun graphe causal, aucune corrélation temporelle large, aucune connaissance spécifique Maison Cognitive ; fail closed si preuve insuffisante.
# Packaging terrain : image privée amd64 + Dockerfile générique + /run.sh ; aucun run_dev72.sh ni Dockerfile dédié.
# Image : ghcr.io/brunofoxmulder/elise-investigator-dev72-private:0.2.0-dev.72
# CI + image : PASS. Validation terrain requise avant toute promotion fonctionnelle. Lecture seule. Dev.54 reste le fallback officiel intact.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?
