name: "Élise Investigator 0.2 Test"
version: "0.2.0-dev.54"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator 0.2, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-dev54-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# DEV.54 TERRAIN 2026-09-02 : base exacte dev.46, ajout limité à l'origine HA Voice.
# Runtime exact issu de la base dev.46
# 1fa3e63852a3ce701b1f230fa0311942d7589d2f.
# Home Assistant Voice assist_satellite.* est reconnu comme origine générique user,
# exactement comme les commandes utilisateur Alexa/IHM existantes.
# Une automation/script déjà établie reste automation/script et n'est jamais relabellisée user.
# Logique lumières off↔on, brightness dev.46 et volets inchangée.
# Qualification dev.54 : 215/215 tests PASS, HA Voice PASS, user existant PASS,
# lumières off↔on PASS, non-régression volets PASS, lecture seule respectée.
# Image : ghcr.io/brunofoxmulder/elise-investigator-dev54-private:0.2.0-dev.54
# Digest OCI : sha256:b7908b067357537acbd3037dd593fc34c02305320823a8414da1db56d8e29e85
# L'App reste strictement en lecture seule vis-à-vis de Home Assistant.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?
