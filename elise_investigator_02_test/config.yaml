name: "Élise Investigator 0.2 Test"
version: "0.2.0-dev.69"
slug: "elise_investigator_02_test"
description: "Candidate privée de test HAOS pour Élise Investigator 0.2, strictement en lecture seule."
arch:
  - amd64
image: "ghcr.io/brunofoxmulder/elise-investigator-dev69-private"
init: false
startup: application
boot: manual
stage: experimental
homeassistant_api: true
# DEV.69 TERRAIN : candidate consolidée issue de la recette naturelle dev.67 et de la correction bornée dev.68.
# Objectifs causaux ciblés : actions finales/différées, conditions conjointement nécessaires, cause fonctionnelle avant trigger technique.
# Préserver strictement les PASS dev.67 : Tineco batterie >99,9 %, chargeur téléphone OFF batterie >99 %, lampes simples/manuelles,
# fermetures volets coucher du soleil, entrée OFF, et chaîne profonde lampe cuisine OFF <- ouverture volet <- conditions météo.
# Aucun graphe, aucune corrélation temporelle générale, aucune recherche causale large. Fail closed si ambigu.
# Présentation française et fonction « depuis quand ? » restent hors périmètre de cette candidate.
# Packaging EXACT terrain dev.62/dev.67 : Dockerfile générique + /run.sh + chmod 0755 + CMD ["/run.sh"].
# Aucun Dockerfile.dev69 ni run_dev69.sh. Image : ghcr.io/brunofoxmulder/elise-investigator-dev69-private:0.2.0-dev.69
# Tests unitaires/digital-twin : PASS. Contrat packaging : PASS. Build/push/manifeste : PASS.
# Première validation terrain : démarrage HAOS + Ingress, puis recette au fil de l'eau. Lecture seule. Dev.54 reste le fallback officiel.
ingress: true
ingress_port: 8099
ingress_entry: /
panel_icon: mdi:magnify-scan
panel_title: Élise Investigator 0.2 Test
panel_admin: true
apparmor: true
ports:
  8099/tcp: null
ports_description:
  8099/tcp: "API locale désactivée par défaut"
options:
  log_level: info
  default_window_minutes: 30
  max_reverse_candidates: 25
  mcp_connect_url: ""
schema:
  log_level: list(debug|info|warning|error)
  default_window_minutes: int(5,180)
  max_reverse_candidates: int(5,100)
  mcp_connect_url: password?
