name: Publish dev.64 private image

on:
  workflow_run:
    workflows:
      - Tests
    types:
      - completed
    branches:
      - dev64-packaging-supervisor-token

permissions:
  contents: read
  packages: write

jobs:
  build-publish:
    if: >-
      github.repository == 'brunofoxmulder/elise-investigator' &&
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.head_branch == 'dev64-packaging-supervisor-token'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout tested dev.64 commit
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and publish amd64 candidate image
        uses: docker/build-push-action@v6
        with:
          context: ./elise_investigator
          file: ./elise_investigator/Dockerfile.dev63
          platforms: linux/amd64
          push: true
          tags: ghcr.io/brunofoxmulder/elise-investigator-dev64-private:0.2.0-dev.64
          build-args: |
            BUILD_VERSION=0.2.0-dev.64
            BUILD_ARCH=amd64

      - name: Verify published image manifest
        run: docker buildx imagetools inspect ghcr.io/brunofoxmulder/elise-investigator-dev64-private:0.2.0-dev.64
