name: Publish dev.38 private image

on:
  push:
    branches:
      - dev38-cover-terminal-direction
    paths:
      - '.github/workflows/publish-dev38-image.yml'
      - 'elise_investigator/**'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-publish:
    if: github.repository == 'brunofoxmulder/elise-investigator' && github.ref == 'refs/heads/dev38-cover-terminal-direction'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout dev.38
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and publish amd64 candidate image
        uses: docker/build-push-action@v6
        with:
          context: ./elise_investigator
          file: ./elise_investigator/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ghcr.io/brunofoxmulder/elise-investigator-dev38-private:0.2.0-dev.38
          build-args: |
            BUILD_VERSION=0.2.0-dev.38
            BUILD_ARCH=amd64

      - name: Verify published image manifest
        run: docker buildx imagetools inspect ghcr.io/brunofoxmulder/elise-investigator-dev38-private:0.2.0-dev.38
