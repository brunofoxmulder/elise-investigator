name: Publish dev.26 private image

on:
  push:
    branches:
      - dev26-mcp-bounded-trace-exploration
    paths:
      - '.github/workflows/publish-dev26-image.yml'
      - 'elise_investigator/**'
      - 'elise_investigator_02_test/config.yaml'
      - 'elise_investigator_02_test/translations/**'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-publish:
    if: github.repository == 'brunofoxmulder/elise-investigator' && github.ref == 'refs/heads/dev26-mcp-bounded-trace-exploration'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout dev.26
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and publish amd64 image
        uses: docker/build-push-action@v6
        with:
          context: ./elise_investigator
          file: ./elise_investigator/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ghcr.io/brunofoxmulder/elise-investigator-dev26-private:0.2.0-dev.26
          build-args: |
            BUILD_VERSION=0.2.0-dev.26
            BUILD_ARCH=amd64

      - name: Verify published image manifest
        run: docker buildx imagetools inspect ghcr.io/brunofoxmulder/elise-investigator-dev26-private:0.2.0-dev.26
