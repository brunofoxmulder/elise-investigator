name: Publish dev.47 private image

on:
  push:
    branches:
      - dev47-latest-primary-state
    paths:
      - '.github/workflows/publish-dev47-image.yml'
      - 'elise_investigator/**'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-publish:
    if: github.repository == 'brunofoxmulder/elise-investigator' && github.ref == 'refs/heads/dev47-latest-primary-state'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout dev.47
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and publish amd64 candidate image
        uses: docker/build-push-action@v6
        with:
          context: ./elise_investigator
          file: ./elise_investigator/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ghcr.io/brunofoxmulder/elise-investigator-dev47-private:0.2.0-dev.47
          build-args: |
            BUILD_VERSION=0.2.0-dev.47
            BUILD_ARCH=amd64

      - name: Verify published image manifest
        run: docker buildx imagetools inspect ghcr.io/brunofoxmulder/elise-investigator-dev47-private:0.2.0-dev.47
