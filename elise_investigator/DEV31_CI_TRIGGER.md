# dev.31 exact-head candidate

Reconstruction finale de l'image privée après validation du contrat stable `/api/v1/investigate` et alignement de l'IHM avec la politique Assist sans enquête profonde synchrone.

- Élise Why inchangée.
- `/api/v1/investigate` : journal causal uniquement, réponse bornée.
- absence de preuve : `indeterminate` immédiat, sans fallback profond dans Assist.
- `/api/v1/investigate/deep` : investigation manuelle approfondie.
- IHM : indique explicitement qu'Assist est journal-only et que l'enquête profonde est manuelle.
- Home Assistant : lecture seule, aucun service mutateur.
