# Dev.25 CI trigger

Ce fichier n'est pas utilisé par l'application et n'a aucun effet runtime.

Marker: USER_TRIGGERED

Pour déclencher GitHub Actions sous l'identité du propriétaire du dépôt, remplacer uniquement `PENDING_USER_TRIGGER` par `USER_TRIGGERED`, puis committer directement dans la branche `dev25-mcp-trace-contract-probe`.
