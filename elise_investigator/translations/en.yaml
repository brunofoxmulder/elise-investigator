configuration:
  log_level:
    name: Log level
    description: Application logging verbosity.
  default_window_minutes:
    name: Default investigation window
    description: Minutes inspected when no observation time is supplied.
  max_reverse_candidates:
    name: Reverse search limit
    description: Maximum candidate configurations inspected as fallback.
network:
  8099/tcp: Optional local API for a conversational AI
