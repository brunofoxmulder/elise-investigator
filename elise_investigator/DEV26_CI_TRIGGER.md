# Dev.26 CI trigger

Ce fichier n'est pas utilisé par l'application et n'a aucun effet runtime.

Marker: PENDING_USER_TRIGGER

Si les commits du connecteur ne déclenchent pas GitHub Actions, remplacer uniquement `PENDING_USER_TRIGGER` par `USER_TRIGGERED`, puis committer directement dans la branche `dev26-mcp-bounded-trace-exploration`.
