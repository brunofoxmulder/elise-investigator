# dev.60 CI trigger

Content-neutral trigger created after the dev.60 publisher workflow was installed on the default branch.

Purpose: obtain a fresh successful `Tests` run whose exact head SHA can be consumed by the `Publish dev.60 private image` workflow.
